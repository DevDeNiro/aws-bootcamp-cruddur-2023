#! /usr/bin/bash


# Substitution string
#Select -> replace
## Doesnt work the same onm all linux system


echo 'db-create'

# NO_DB_CONNECTION_URL=$(sed 's/\/cruddur//g' <<<"$CONNECTION_URL")
# createdb cruddur $NO_DB_CONNECTION_URL

NO_DB_CONNECTION_URL=$(sed 's/\/cruddur//g' <<<"$CONNECTION_URL")
psql $NO_DB_CONNECTION_URL -c 'CREATE database cruddur'; 